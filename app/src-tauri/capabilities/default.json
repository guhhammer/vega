{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Vega's window needs only its own events. No plugin surface is granted.",
  "windows": ["main"],
  "permissions": ["core:default", "core:event:default"]
}
